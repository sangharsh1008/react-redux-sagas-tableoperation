// @flow
import
  * as actions
 from '{{featureDir}}/{{lowerCase feature}}/{{featureName}}Actions';

import { testAsyncAction } from 'common/__support__/testUtils';

// test each one
describe('{{featureName}} actions', () => {
  // add sync action tests here:
});
